import { siteUrl } from '../hooks/useRoute';
import { useTranslation } from 'react-i18next';
import { languages, selectLanguage } from '../i18n';
import { Logo, PlaceholderLink, SocialIcon, socialPlatforms } from './Shared';
export function Footer() {
  const { t, i18n } = useTranslation();
  return <footer className="footer"><div className="footer-top"><Logo/><nav className="footer-pages" aria-label={t('footer.pages')}><h2>{t('footer.pages')}</h2><a href={siteUrl('/#home')}>{t('nav.home')}</a><a href={siteUrl('/activiteiten')}>{t('nav.nearby')}</a><a href={siteUrl('/pluspunten')}>{t('nav.benefits')}</a><a href={siteUrl('/reserveren')}>{t('nav.book')}</a><a href={siteUrl('/#gallery')}>{t('nav.gallery')}</a></nav><nav className="footer-links" aria-label={t('footer.links')}><h2>{t('footer.links')}</h2>{['instagram','facebook','rental'].map(name => <PlaceholderLink key={name}>{t(`social.${name}`)}</PlaceholderLink>)}</nav></div><div className="footer-divider"/><div className="footer-controls"><div className="language-switcher" role="group" aria-label={t('footer.language')}>{languages.map(language => <button key={language} lang={language} aria-pressed={i18n.language === language} onClick={() => selectLanguage(language)}>{t(`languages.${language}`)}</button>)}</div><nav className="social-links" aria-label={t('footer.social')}>{socialPlatforms.map(name => <PlaceholderLink key={name} aria-label={t(`social.${name}`)}><SocialIcon name={name}/></PlaceholderLink>)}</nav></div><nav className="legal-links" aria-label={t('footer.legal')}>{['legal','cookies','privacy'].map(name => <PlaceholderLink key={name}>{t(`footer.${name}`)}</PlaceholderLink>)}</nav></footer>;
}
